//- Every asset triggers its own http request, there will be separate http requests to get the favicon and css styles
    //- body 
    //-     //- Buffered Code (visible in output)
    //-     h1= tour 
    //-     h2= user.toUpperCase()
    //-     p  This is just some text

    //-     //- Unbuffered Code (Not visible in output)
    //-     - const x = 9
    //-     h3= x * 2

doctype html   
html
    head 
        block head
            link(rel="stylesheet" href="/css/style.css")
            link(rel="shortcut icon" href="/img/favicon.png" type="image/png")
            link(href="https://fonts.googleapis.com/css?family=Lato:300,300i,700" rel="stylesheet")
            script(src="https://js.stripe.com/v3/")
            script(src="/js/bundle.js" defer) 
            //- script(src="https://unpkg.com/axios@1.6.7/dist/axios.min.js")
            //- script(src="/js/login.js" defer)

            meta(charset="UTF-8")
            meta(name="viewport" content="width=device-width, initial-scale=1.0")
            title Natours | #{title}
    body 
        // HEADER
        include _header
            
        // CONTENT
        block content 
            h1 This is placeholder heading
        
        // FOOTER
        include _footer
            
    

    


